import React from 'react';
import renderer from 'react-test-renderer';

import Dice from '../generic-dice.js';

describe('GenericDice', () => {
  it('never fails', () => {
    expect(true).toEqual(true);
  });
});
